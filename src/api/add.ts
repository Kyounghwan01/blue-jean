// import { getFirestore, collection, getDocs } from "firebase/firestore/lite";

// const db = getFirestore(app);

const add = async (key: string, payload: any) => {
  return key;
  // return await db.collection(key).add(payload);
};

export default add;
